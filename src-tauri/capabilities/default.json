{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Base permissions for the main application window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
